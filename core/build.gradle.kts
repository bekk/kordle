dependencies {
  implementation("com.badlogicgames.ashley:ashley:${project.property("ashleyVersion")}")
  implementation("com.badlogicgames.gdx:gdx-ai:${project.property("aiVersion")}")
  implementation("com.badlogicgames.gdx:gdx-box2d:${project.property("gdxVersion")}")
  implementation("com.badlogicgames.gdx:gdx-freetype:${project.property("gdxVersion")}")
  implementation("com.badlogicgames.gdx:gdx:${project.property("gdxVersion")}")
  implementation("com.kotcrab.vis:vis-ui:${project.property("visUiVersion")}")
  implementation("io.github.libktx:ktx-actors:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-ai:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-app:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-artemis:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-ashley:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-assets-async:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-assets:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-async:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-box2d:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-collections:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-freetype-async:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-freetype:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-graphics:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-i18n:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-inject:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-json:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-log:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-math:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-preferences:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-reflect:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-scene2d:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-style:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-tiled:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-vis-style:${project.property("ktxVersion")}")
  implementation("io.github.libktx:ktx-vis:${project.property("ktxVersion")}")
  implementation("net.onedaybeard.artemis:artemis-odb:${project.property("artemisOdbVersion")}")
  implementation("org.jetbrains.kotlin:kotlin-stdlib:${project.property("kotlinVersion")}")
  implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:${project.property("kotlinxCoroutinesVersion")}")
  implementation(project(":shared"))
  implementation("io.github.berstanio:gdx-svmhelper-annotations:${project.property("graalHelperVersion")}")
}
